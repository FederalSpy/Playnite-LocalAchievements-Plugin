<Window x:Class="LocalAchievements.AchievementToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Achievement Unlocked" 
        Height="80" Width="350"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ShowActivated="False" 
        Focusable="False"
        MouseLeftButtonUp="Window_MouseLeftButtonUp">

    <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#444" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="ImgIcon" Grid.Column="0" Margin="10" RenderOptions.BitmapScalingMode="HighQuality"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0,10,0">
                <TextBlock x:Name="TxtTitle" Foreground="#FFD700" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtDesc" Foreground="#CCCCCC" FontSize="11" TextWrapping="Wrap" MaxHeight="32" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>