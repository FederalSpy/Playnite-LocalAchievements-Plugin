<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="170" Width="520"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    ShowActivated="False"
    Focusable="False">

	<Window.Triggers>
		<EventTrigger RoutedEvent="FrameworkElement.Loaded">
			<BeginStoryboard>
				<Storyboard>
					<DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.4"/>
					<DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="1" To="0" BeginTime="0:0:5" Duration="0:0:0.6"/>
				</Storyboard>
			</BeginStoryboard>
		</EventTrigger>
	</Window.Triggers>

	<Border Background="#CC1F1F1F"
            CornerRadius="28"
            BorderBrush="#FFD700"
            BorderThickness="2"
            Margin="10">
		<Border.Effect>
			<DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.75"/>
		</Border.Effect>

		<Grid Margin="16,12,16,16">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="140"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- CONTENEDOR CIRCULAR -->
			<Grid Grid.Column="0"
                  Width="100" Height="100"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Margin="8,0">

				<!-- Borde blanco -->
				<Ellipse Stroke="White"
                         StrokeThickness="2"/>

				<!-- Imagen del logro -->
				<Image Source="{Binding ImageUrl}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality">
					<Image.Clip>
						<EllipseGeometry Center="50,50"
                                         RadiusX="48"
                                         RadiusY="48"/>
					</Image.Clip>
				</Image>
			</Grid>

			<!-- TEXTOS -->
			<StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="12,0,8,0">

				<TextBlock Text="🏆 LOGRO DESBLOQUEADO"
                           Foreground="#FFD700"
                           FontSize="13"
                           FontWeight="Bold"
                           Margin="0,0,0,8"/>

				<TextBlock Text="{Binding Title}"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           MaxHeight="70"
                           LineHeight="26"
                           Margin="0,0,0,10"/>

				<TextBlock Text="{Binding Description}"
                           Foreground="#D0D0D0"
                           FontSize="15"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           MaxHeight="90"
                           LineHeight="22"/>
			</StackPanel>
		</Grid>
	</Border>
</Window>
